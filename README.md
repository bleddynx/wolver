# Create a new React project with Vite
npm create vite@latest kontakt -- --template react

# Enter the folder
cd kontakt

# Install dependencies
npm install lucide-react

# Replace src/App.jsx with the Kontakt code

# Start developing
npm run dev
